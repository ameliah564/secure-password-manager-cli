# password_manager
Password manager storing details of the URL, username and password through a command line interface.

Created using Python and PostgreSQL

To set up in a command line, run the commands:
* venv\Scripts\activate
* python connect.py
